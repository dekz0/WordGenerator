# 📄 WordGenerator - Генератор Word документов

Программа для массовой генерации Word документов из Excel данных и Word шаблона.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Запуск
```bash
python app.py
```

### Сборка exe (для Windows)
```bash
# Windows
build.bat

# macOS/Linux
./build.sh
```

---

## 🧩 Общая идея проекта

Программа принимает на вход:
1. **Excel файл** - где колонки это названия переменных, а строки - данные для подстановки
2. **Word шаблон** - с переменными в формате Jinja2: `{{ переменная }}`

Результат: создаётся N документов Word (по количеству строк в Excel).

**Пример:** 1000 строк в Excel → 1000 готовых Word документов.

---

## 📁 Структура проекта

```text
WordGenerator/
│
├── app.py                  # Точка входа (GUI)
│
├── ui/
│   └── main_window.py      # Современный UI на CustomTkinter
│
├── core/
│   ├── loader.py           # Загрузка Excel (pandas)
│   ├── validator.py        # Проверка данных и шаблона
│   ├── template.py         # Работа с Word шаблоном (docxtpl)
│   ├── generator.py        # Генерация документов (многопоточно)
│   └── exporter.py         # Сохранение файлов
│
├── services/
│   ├── progress.py         # Callback прогресса
│   └── logger.py           # Логирование
│
├── config/
│   └── settings.py         # Настройки приложения
│
├── templates/              # Примеры шаблонов
├── output/                 # Результаты генерации
│
├── requirements.txt        # Зависимости Python
├── WordGenerator.spec      # Конфигурация PyInstaller
└── build.bat/.sh           # Скрипты сборки
```

---

## 🏗️ Архитектура (SOLID)

### Single Responsibility
Каждый модуль отвечает за одну задачу:
- `loader.py` → только загрузка Excel
- `template.py` → только работа с шаблоном
- `exporter.py` → только сохранение файлов

### Open/Closed
Используются Protocol-классы для интерфейсов, что позволяет легко добавить:
- Другие форматы данных (CSV, JSON)
- Другие форматы шаблонов

### Dependency Inversion
UI не зависит от core-логики напрямую. Связь через callbacks и интерфейсы.

---

## ⚡ Производительность

- **Многопоточная обработка** через `ThreadPoolExecutor`
- **Настраиваемое количество потоков** (по умолчанию 4)
- **Обработка по строкам**: ошибка в одной строке не останавливает весь процесс
- **Лёгкий UI**: CustomTkinter вместо тяжёлых фреймворков

---

## 🎨 UI Features

- Современный тёмный/светлый интерфейс (auto по системе)
- Прогресс-бар в реальном времени
- Выбор файлов через диалоги
- Понятные сообщения об ошибках
- Возможность открыть папку с результатами

---

## 📝 Формат шаблона Word

Используйте переменные в формате Jinja2:

```
Договор № {{ Номер }}

Дата: {{ Дата }}

Клиент: {{ ФИО }}
Сумма: {{ Сумма }} рублей
```

**Важно:** Названия переменных должны совпадать с названиями колонок в Excel.

---

## ⚠️ Обработка ошибок

Вместо технических ошибок:
```
KeyError: 'Дата_договора'
```

Пользователь увидит:
```
В Excel отсутствует колонка: Дата_договора
```

---

## 🔧 Конфигурация

Файл `config/settings.py`:

```python
MAX_ROWS = 5000              # Лимит строк
MAX_WORKERS = 4              # Потоков для обработки
FILENAME_PATTERN = "Документ_{index}"  # Паттерн имени файла
```

---

## 📦 Зависимости

- **customtkinter** - современный UI
- **pandas** - работа с Excel
- **docxtpl** - шаблоны Word (Jinja2)
- **pyinstaller** - сборка exe

---

## 🧪 Тестирование

Core-модули тестируются без UI:

```python
from core.loader import ExcelLoader
from core.template import WordTemplate

loader = ExcelLoader()
data = loader.load(Path('data.xlsx'))

template = WordTemplate(Path('template.docx'))
content = template.render(data[0])
```

---

## 📈 Расширяемость

Без изменения архитектуры можно добавить:
- ✅ PDF генерацию
- ✅ ZIP архивирование результатов
- ✅ Несколько шаблонов
- ✅ Предпросмотр документа
- ✅ Web-версию / CLI

---

## 🎯 Pipeline

```
Excel → DataLoader → Validator → Generator → Template → Exporter
                              ↘ Logger + Progress ↙
```

