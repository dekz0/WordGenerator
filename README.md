# 🧩 Общая идея проекта

Стоит задача создать программу которая на взод принимает excel файл где колонки это название переменных, строки это данные которые нужно вставить в word файле. Может быть 1000 строк и тогда нужно прогнать(подставить) их всех и создать 1000 ворд документов. есть шаблонный ворд файл с переменными по типу {переменная}.

**Pipeline:**

```
Excel → DataLoader → Validator → TemplateEngine → DocumentGenerator → Export
                              ↘ Logger ↙
```

---

# Структура проекта

```text
doc_generator/
│
├── app.py                  # Точка входа (GUI)
│
├── ui/
│   └── main_window.py      # Окно, кнопки, прогресс
│
├── core/
│   ├── loader.py           # Загрузка Excel
│   ├── validator.py        # Проверка данных и шаблона
│   ├── template.py         # Работа с переменными {var}
│   ├── generator.py        # Генерация Word
│   └── exporter.py         # Сохранение файлов
│
├── services/
│   ├── progress.py         # Callback прогресса
│   └── logger.py           # Логи
│
├── config/
│   └── settings.py         # Настройки
│
├── templates/
│   └── example.docx        # Пример шаблона
│
├── output/
│   └── (результаты)
│
└── requirements.txt
```

---

# 🔹 Слои ответственности

## 1️⃣ UI слой (`ui/`)

**Ничего не знает о Word и Excel**

Отвечает за:

* выбор файлов
* кнопку «Сгенерировать»
* прогресс
* сообщения об ошибках

---

## 2️⃣ Core слой (`core/`)

**Вся бизнес-логика**

### 🔸 `loader.py`

Загружает Excel → `List[dict]`

---

### 🔸 `validator.py`

Проверяет:

* Excel не пуст
* все `{переменные}` есть в колонках
* нет пустых обязательных значений

**Ошибки → понятные сообщения для пользователя**

---

### 🔸 `template.py`

Работает с шаблоном Word:

* извлекает `{переменная}`
* заменяет значения


---

### 🔸 `generator.py`

Главный оркестратор:

Обрабатывает:

* ошибки одной строки
* продолжение процесса

---

### 🔸 `exporter.py`

* создаёт папки
* формирует имена файлов


---

## 3️⃣ Services слой (`services/`)

**Кросс-функциональные вещи**

### 🔸 `progress.py`


UI подписывается на него.

---

### 🔸 `logger.py`

* лог-файл рядом с exe
* ошибки, предупреждения

---

## 4️⃣ Config (`config/`)

Централизованные настройки:

```python
MAX_ROWS = 5000
FILENAME_PATTERN = "Договор_{ФИО}.docx"
```

---

# 🔄 Поток выполнения

```text
UI
 ↓
Loader → Validator → Generator
                    ↓
               Template → Exporter
                    ↓
                 Logger
```

---

# 🧠 Ключевые архитектурные решения

### ✅ Строгое разделение UI / логики

→ можно потом:

* убрать UI
* сделать web-версию
* сделать CLI

---

### ✅ Работа по строкам

→ если строка №357 упала:

* остальные 999 сгенерируются
* ошибка попадёт в лог

---

### ✅ Читаемые ошибки

Вместо:

```
KeyError: 'Дата_договора'
```

Пользователь увидит:

```
В Excel отсутствует колонка: Дата_договора
```

---

# ⚙️ Расширяемость (на будущее)

Без переделки архитектуры можно добавить:

* PDF генерацию
* ZIP архив
* несколько шаблонов
* предпросмотр документа
* запуск из браузера

---

# 🧪 Тестируемость

`core/` тестируется **без UI**:

```python
def test_template_render():
    ...
```

---

# 🎯 Минимальный MVP (что делать первым)

1. `loader.py`
2. `template.py`
3. `generator.py`
4. простой UI (2 кнопки)
5. PyInstaller

---

